pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Ensure repo is cloned into workspace
                git branch: 'main', url: 'https://github.com/bhagyajkumar/jenkins-learn.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Building project...'
                // your build commands here
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // your test commands here
            }
        }
    }
}
